function conn_oracle(){  
    eel.connect_oracle(document.getElementById("oracle_host").value,document.getElementById("oracle_port").value,document.getElementById("oracle_sid").value) // call the demo function which we have created in the main.py file
}